pipeline {
   agent any

   stages {
      stage('Build & Test') {
         steps {
            git 'https://<GITHUB_TOKEN>@github.com/newtonbeck/curso-devops.git'

            sh "mvn clean test"
         }
      }
   }
}
